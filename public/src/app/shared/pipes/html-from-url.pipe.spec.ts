import { HtmlFromUrlPipe } from './html-from-url.pipe';

describe('HtmlFromUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlFromUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
